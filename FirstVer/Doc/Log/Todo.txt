\\nas.xinje.cc\研发训练营\资料共享\进度管理

TCP 主
RUT 从

故事点（思维导图？）（项目细化）

将一个较大项目拆分为多个小型模块

不可拆分的小型模块为1 ，被称为故事点

测试点
测试样例

测试：

如果两个从设备ID相同？
正常断开连接、异常断开连接？

主                                                             从
设备读取到多个寄存器修改如何显示                		 Read  /  Write       

昨日工作（初步撰写产品规格书）

今日计划;

1：完成产品规格书的撰写。

2：完成产品方案书的撰写。

3：优化完善学习文档。




Modbus工控设备
C#新建类调用控件属性
显示该控件访问具有一定保护不可访问

发现控件在新建时的属性均为Private
将需要在新建类调用的控件权限(Modifiers)改为Public即可


C#中
在子类函数前定义public修饰符的变量试图当做全局变量

但发现程序执行，引用到该变量时，其值总为初始化的值

应修饰为public static


控件问题

尽管将控件权限改为公开后，子类访问该控件得到textbox内值始终不变

最终选择将子类属性改为public partical 无条件访问控件及属性值



尚未完成：

只有回车键按下，数据才会发送出去


8 29

数据帧越界，提示重新输入，但数据帧还是发出去了。


8 31 
修复了关闭窗体时，TCP初始化显示无法操作已释放控件的BUG。

修复了提示数据帧错误但还是发出去的BUG。

修复了数据帧错误，传输标识依然+1的BUG。

修复了不选择功能码时，功能码默认为0x00的BUG。